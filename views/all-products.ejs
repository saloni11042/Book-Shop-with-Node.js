<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>All Products</h1>
    
    <% for (let book of books) { %>
        <h2><%= book.book_name %></h2> 
        <p>Author: <%= book.author_name %></p> 
        <p>Price: <%= book.price %></p>
        <img src="<%= book.image %>" alt="Book Image" style="max-width: 150px;">
        <img src="../my-uploads/<%= book.book_image %>" alt="Book Image" style="max-width: 150px;">
        <a href="/admin/update-product/<%=book.id%>">Update Product</a>
            
        
        <form action="/admin/delete-product/<%=book.id%>" method="post">
            <button type="submit">Delete Product</button>
        </form>
        
    <% } %>
</body>
</html>
